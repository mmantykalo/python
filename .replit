run = "uvicorn main:app --host 0.0.0.0 --reload"
modules = ["python-3.12"]

hidden = [".pythonlibs"]

[nix]
channel = "stable-24_05"
packages = ["libxcrypt", "postgresql"]

[deployment]
run = ["sh", "-c", "uvicorn main:app --host 0.0.0.0 --reload"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 8000
externalPort = 80
